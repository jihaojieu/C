#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int Factor(int num) {
	if (num > 9) {
		Factor(num / 10);
	}
	printf("%d\n", num % 10);
}
int main() {
	int num = 0;
	scanf("%d", &num);
	Factor(num);
	system("pause");
	return 0;
}#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int Factor(int num) {
	if (num > 9) {
		Factor(num / 10);
	}
	printf("%d\n", num % 10);
}
int main() {
	int num = 0;
	scanf("%d", &num);
	Factor(num);
	system("pause");
	return 0;
}#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int Factor(int num) {
	if (num > 9) {
		Factor(num / 10);
	}
	printf("%d\n", num % 10);
}
int main() {
	int num = 0;
	scanf("%d", &num);
	Factor(num);
	system("pause");
	return 0;
}#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int Factor(int num) {
	if (num > 9) {
		Factor(num / 10);
	}
	printf("%d\n", num % 10);
}
int main() {
	int num = 0;
	scanf("%d", &num);
	Factor(num);
	system("pause");
	return 0;
}