#include <stdio.h>
#include <stdlib.h>
char Reverse_String(char string[]) {
	int ret = sizeof(string) / sizeof(string[0]);
	for (int i = 1; i <= ret; i++) {
		return Reverse_String;
	}
}
int main() {
	int i;
	char string[] = "abcd";
	printf("%c\t", string[i]);
	system("pause");
	return 0;
}